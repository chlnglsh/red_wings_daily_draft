// Injected at build time by vite.config.ts (`define`) from the selected team's
// identity, so the server can name the team without importing the symlinked app/ tree.
declare const __TEAM_NAME__: string;
